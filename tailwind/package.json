{
  "name": "tailwind",
  "description": "Use Fylgja's CSS with Tailwind",
  "version": "1.0.0",
  "private": true,
  "repository": "fylgja/examples",
  "license": "MIT",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run dev",
    "dev": "live-server --no-css-inject & npx tailwindcss -i ./src/main.css -o ./dist/main.css --watch",
    "build": "npx tailwindcss -i ./src/main.css -o ./dist/main.css"
  },
  "dependencies": {
    "fylgja": "^1.3.0"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "tailwindcss": "^3.3.3"
  }
}
